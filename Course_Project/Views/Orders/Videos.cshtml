@model List<Video>

<h2>@ViewBag.CourseTitle</h2>
<ul>
    @foreach (var item in Model)
    {
        <li>
            @item.Title -
            <a href="@item.Url" target="_blank">مشاهده</a>
        </li>
    }
</ul>